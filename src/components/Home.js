import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class Home extends Component {
  render() {
    return (
      <div>
        <h1>Home</h1>
      </div>
    );
  }
}

Home.propTypes = propTypes;

export default Home;
