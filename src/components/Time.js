import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class Time extends Component {
  render() {
    return (
      <div>
        <h1>Time</h1>
      </div>
    );
  }
}

Time.propTypes = propTypes;

export default Time;
